// The backend is minimal and doesn't need initialization
// All data comes from Binance API in real-time
export function useInitializeApp() {
  // No-op: backend only stores active/closed trades
  // Market data comes from Binance, categorization happens in frontend
}
